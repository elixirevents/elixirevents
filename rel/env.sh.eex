#!/bin/sh

# Pin a stable node name so `bin/elixir_events remote` can attach
# to the running server. Without this, the release uses the
# container's hostname, which changes every deploy and breaks
# remote shells.
export RELEASE_DISTRIBUTION=sname
export RELEASE_NODE=elixir_events
